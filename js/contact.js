var React = require('react');

const Contact = React.createClass({

    handleClick() {
        
    },

    render() {
        return(
            <div>
                Contact
            </div>
        )
    }
});

module.exports = Contact;